Django==4.2.6

# Used for postgres connection
psycopg==3.1.16
psycopg-binary==3.1.16

# Used for image media files by django
Pillow==10.0.1

# Used to implement REST API
djangorestframework==3.14.0

# Used to authenticate user using JWTs for api 
djangorestframework-simplejwt==5.3.0

# Used to support CORS headers
django-cors-headers==4.2.0

# Used to support both WSGI and ASGI for django and django channels respectively
daphne==4.0.0

# Used to support WebSocket protocol
channels==4.0.0

# Used to read metadata mainly from audio files
mutagen==1.47.0

# Used to generate fake data
Faker==19.10.0